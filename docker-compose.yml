#docker-compose file
version: '3'

services:
  nest-auth:
    container_name: nest-auth
    image: nest-auth:1.0.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3002:3002'
    env_file:
      - .env
